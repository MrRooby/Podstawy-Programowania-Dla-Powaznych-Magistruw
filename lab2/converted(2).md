LABORATORY 2    DESIGNING CONTROLLER CLASSES    Programming topics covered:    • Classes and encapsulation.    • Inheritance and dynamic object - oriented polymorphism.    • Programming principles: DRY, YAGNI, and KISS.    • Elementary code refactoring (method/class   separation).    • Object serialization and deserialization.    • Creating a two - tier application.    Mathematical modeling topics covered:    • Simulating a discrete PID controller.    Skills to master:    • Creating data abstractions to model a given problem.    • Using inheritan ce and polymorphism.    • Implementing an algorithm to simulate the operation of a PID controller.    • Basic code testing and refactoring.     • Creating a two - tier application (presentation and control layer, and data layer).    Command symbols:    ■  Mandatory commands r equired for the entire project, which is covered in all course   topics.    ●  Optional commands allow for better grades.         Tasks to be performed    Controller class:    ■  Define the ControllerPID class as a derivative of the ObjectSISO class. The class   should contain   the fields necessary to simulate a PI controller and, optionally, a full   PID controller. The following are required:    -   proportional part setting (P)  –   gain (k),    -   integral part setting (I)  –   integral constant (TI),    -   (●)  derivative part setting (D)  –   derivative constant (TD),  

-   integral part memory (current "integral" of the error),    -   (●)  derivative part memory (previous error value).    All of the above fields are real numbers.    ■  Define a basic class interface that includes unit setters for the s ettings . Do not   define setters for "memory fields," as knowledge of them is not required outside the   ControllerPID class. Field getters are also not needed. Tips: Note that all setpoints   can only accept non - negative values. Therefore, following the DRY principle,   it's   worth defining a single private setter that allows you to set any setpoint, which will   only allow you to enter a non - negative value or zero otherwise. Use a private setter in   field unit setters, and use the individual field unit setters in the constr uctor.    To write a universal non - negative setter, you can pass it two arguments: a modifiable   reference to the field being set and the value being set.    ■  Define argument constructors that allow you to set the setpoint values:    -   only the proportional part  (P),  -   both the proportional (P) and integral (I) parts,    -   (optional) all parts of the full PID controller.    Ensure that the values  of the "memory fields" are always set to  0.0 .    Tips:   Remember to force the "memory fields" to 0.0 when creating an instance.   This   can be done by assigning default values  to them in the class body (the simplest   method), or by resetting them in the constructor's initialization list. It's best not to   "clutter" the constructor body by setting these fields to zero.    Note that this  command can be executed by defining only one constructor and   assigning appropriate default values  to its arguments. Unused parts of the controller   should be set to 0.0.    ■  Define an overload of the virtual method simulate from the ObjectSISO class, which   temporarily returns 0.0. Remember the override specifier.     ■  Implement the simulate method so that it correctly simulates the operation of the P -  type controller, according to the formula:    𝑢 𝑖   = 𝑘 𝑒 𝑖 ,    where  𝑢 𝑖   is the control signal, ei is the error signal, and k is the gain (setpoint). Test   the P controller using the tests from the "lab2.cpp" file.    Tips:   Note that in the controller, the symbol  𝑒 𝑖   denotes the error signal value at the ith   simulation instant ( this is not the same as a disturbance in the object). This time, the   𝑢 𝑖   signal is the controller output (in the object, it is the input).  

Tips:  Pay attention only to the test results for the P controller; the other test results at   this stage should yield  negative results. If your implementation does not include a full   PID controller, comment out the test calls for it.    ■  Extend the simulate method so that it correctly simulates the operation of a PI   controller, according to the formula:    𝑢 𝑖   = 𝑘 𝑒 𝑖   + ∑ 𝑒 𝑗  𝑖  𝑗 = 0     Where: TI is the integration constant. Test the operation of the PI controller using the   tests in the "lab2.cpp" file.    Tips:   Note that integration in the discrete controller is implemented using a sum. This   sums the error values  from the beginning o f the simulation to the current time instant.   Also note that the TI constant appears in the denominator in the formula, and we   allow its value to be 0.0 (indicating integration disabled). When simulating these   parts, check whether TI > 0 and return 0.0 oth erwise.    ■  Add a serialization method and a deserialization constructor to the RegulatorPID   class, operating similarly to those in the ModelARX class (previous tutorial). Be sure   to use the same serialization method as before.    Refactoring and Testing:    ●  Perform a simple code refactoring, separating the simulation of the P part and the I   part into private methods. The simulate method should only return the sum of the   results of these methods.    Tips:   These methods should accept the error as input and return   the contribution of a   given part of the controller to the resulting control.    ●  Add the D part to the simulation so that the full PID controller can be simulated   according to the formula:     𝑢 𝑖   = 𝑘 𝑒 𝑖   +   1  𝑇 𝐼  ∑ 𝑒 𝑗   + 𝑇 𝐷 ( 𝑒 𝑖   − 𝑒 𝑖 = 1 )  𝑖  𝑗 = 0        Tips:   Note that th e D part contribution must be calculated before storing the current   derivative error.    ■  Write a function that simulates one step of a feedback loop. The function should   accept the ControllerPID and ModelARX references, along with the current setpoint   sampl e. First, calculate the error as the difference between the setpoint and the   previous ModelARX output value (see the guidelines). Then, simulate one step of the   controller's operation and pass the resulting control to the ModelARX simulation of 

one step. R eturn the ModelARX output (hereinafter referred to as the controlled   value) via return.    Tips:   It is crucial that the error calculation is based on the current setpoint sample   and the previous output signal value from the object. This can be achieved by us ing a   static variable in the function.    However, it is important to remember that a function written this way is not suitable   for simulating more than one loop at a time. The static variable cannot be easily   reset, so this is only a temporary solution to t est how the controller interacts with the   object. We will replace it with a dedicated class in the future.      ■  Use the written loop simulation function to simulate 20 - 30 control steps for a   setpoint of 0.0 at time i = 0 and 1.0 in subsequent steps. Assume th e object   parameters (a1 =  - 0.4, b0 = 0.6, transport delay 1, and standard deviation of 0.0).    Conduct the following simulations:    1)   Only the P controller for k = 0.5  –   the controlled variable should not reach the   setpoint (so - called steady - state error). Thi s is the correct operation of a P - type   controller.    2)   Only the P controller for k = 2.0;  –   the controlled variable should enter into clear   oscillations with increasing amplitude  –   the system has been destabilized.     3)   A PI controller with k = 0.5 and TI = 1 0.0 should slowly approach the setpoint, but   not reach it within 30 simulation steps. The controller works, but very slowly.    4)   A PI controller with k = 0.4 and TI = 2.0 should reach the setpoint within the   observed range, but at the cost of a noticeable o vershoot around the 5th or 6th   sample.    The prepared function should facilitate a smaller implementation of the feedback   simulation class. It's important to note that the control loop itself introduces a single -  step delay, just like ModelARX   itself. These are completely valid features that give   the simulation cause - and - effect credibility.    ●  Add a unit test to the project to verify correct serialization/deserialization.    ●  Add a test class for the controller to the project, similarly to the one   for ModelARX,   and move the tests to it. This is important for maintaining good code organization,   which will become increasingly complex with each class.    Notice that the static methods for comparing sequences and displaying sequence   differences now appear identically in both test classes (for ModelARX and for   RegulatorPID). Separate them into a purely static base class, TESTS, which you can   place in the tra nslation unit of the ObjectSISO class, as both classes will "include" it.   Creating a two - tier application:  

■  Add a user interface class to the program, responsible for communicating with the   user and controlling the program flow. From now on, this class wi ll constitute the   presentation and control layer, while the remaining classes will constitute the data   layer. Choose whether you want to create an interface:    -   (●)  graphical (GUI), use a ready - made framework, e.g., Qt, ImGUI, etc.    -   text - based (CLI).    If yo u choose a GUI, display the simulation results on graphs: error, control, and   controlled value (plot the setpoint on the controlled value graph).    Tips:   The Qt package is cross - platform (Linux, Mac, Windows) and free for open -  source projects. It offers a w ealth of ready - made libraries that allow for the quick and   easy creation of fully functional graphical applications. In the Qt package, creating a   new Widget application project automatically creates the MainWindow presentation   class.    If you choose a CLI,   ensure this class is a singleton (only one instance can be   created in the program).    Tips:   The simplest solution is to create a purely static class. This way, you don't   create any instances at all and ensure that its state cannot be unnecessarily   duplicat ed. You can also read about the singleton design pattern and implement it.     ■  In the interface class, for now, add only simple functionalities related to the   creation, parameterization, and simulation of a system with a single discrete object   and a controll er.    Tips:   Ensure that all communication with the user takes place exclusively through   this class. This class should delegate simulation tasks to the data layer classes and   receive results from them. Remove all I/O operations from the data layer classes. I f   you're using the CLI, you can leave the diagnostic messages, but use the stderr error   stream to display them to clearly indicate that they are not part of the interface. Using   Qt, you can obtain diagnostic messages using the QInfo class from the qdebug   l ibrary.    NOTE: The interface class will be gradually developed in each class, as we'll be   adding new functionality to the project. Therefore, it's not worth leaving it to the very   end, even if you're working on it during the course.  